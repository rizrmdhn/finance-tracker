/** Tables registered as Conflict-free Replicated Relations via cr-sqlite */
export const CRDT_TABLES = [
	"accounts",
	"app_settings",
	"budgets",
	"categories",
	"recurrences",
	"transactions",
] as const;
